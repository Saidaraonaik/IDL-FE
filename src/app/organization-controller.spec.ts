import { OrganizationController } from './organization-controller';

describe('OrganizationController', () => {
  it('should create an instance', () => {
    expect(new OrganizationController()).toBeTruthy();
  });
});
